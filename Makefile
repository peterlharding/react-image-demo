

HOST         := $(shell grep -s HOST=          .env | sed 's/.*=//')
APP_PORT     := $(shell grep -s APP_PORT=      .env | sed 's/.*=//')


# -----------------------------------------------------------------------------

chk-env:
	@echo "      HOST |${HOST}|"
	@echo "  APP_PORT |${APP_PORT}|"


# -----------------------------------------------------------------------------

install:
	npm install


# -----------------------------------------------------------------------------

dev:
	npm run dev

run:
	npm start


# -----------------------------------------------------------------------------

