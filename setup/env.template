# Copy to .env in the project root and adjust for your machine.
# The values below are generic defaults; change APP_PORT if 3000 is already in use.
# Shell environment variables override values in .env.

HOST=127.0.0.1

APP_PORT=3000
